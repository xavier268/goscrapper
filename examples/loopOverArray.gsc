// Loop Over an Array

// Initialize an array 'a' with the years 2023, 2024, and 2025
a = [2023, 2024, 2025];

// Begin a loop to iterate over each element in the array 'a'
FOR i IN a;                            // 'i' sequentially takes each value from 'a'
    PRINT i;                           // Output the current value of 'i' to the console
    RETURN LAST "done";                // Indicate completion after all elements are printed
